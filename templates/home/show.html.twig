{% extends 'base.html.twig' %}

{% block title %}Band Bridge{% endblock %}

{% block body %}
    <div class="container-fluid bg-5">
        <div class="row">
            <div class="col-2"></div>
            <div class="col-8 text-white">
                <h1 class="mt-5 ff-2">{{ event.name }}<span class="ps-3 fs-5 ff-1">{{ event.organization.name }}</span></h1>

                <p>{{ event.description }}</p>
                <p>Ubicación: {{ event.location }}</p>

                {% for detail in details %}
                    <p>{{ detail.requiredInstrument.name }}<span> {{ detail.quantity }}</span></p>
                {% endfor %}
            </div>
            <div class="col-2"></div>
        </div>
    </div>
{% endblock %}